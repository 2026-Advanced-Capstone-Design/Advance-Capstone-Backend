name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'Advance-Capstone-Backend/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./Advance-Capstone-Backend
          push: true
          tags: jaewon0009/newslens-backend:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/newslens
            docker-compose -f docker-compose.prod.yaml pull backend
            docker-compose -f docker-compose.prod.yaml up -d --no-deps backend
            docker image prune -f
