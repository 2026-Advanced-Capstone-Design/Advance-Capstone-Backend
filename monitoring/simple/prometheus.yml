global:
  scrape_interval: 15s

scrape_configs:
  # Spring Boot 백엔드 (actuator/micrometer 지표)
  - job_name: 'backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['newslens-backend:8080']
